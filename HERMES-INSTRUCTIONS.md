# Hermes 操作指令（Claude Code 下发）

> 批次: **第七十二批（最新）** · 更新: 2026-09-08 · 来源: Claude Code
> 状态: **NEWGEN 四色块精确色重上色 · 待执行部署**

---

## 第七十二批：NEWGEN 四色块按精确 HEX 重上色

用户选择精确色方案（示例值作为正式色）。CC 已重上色（提交 `7dc055f`）：

- 目标四色（按 2×2 左上→右下）
  - 绿 `#8EC43F`、黄橙 `#FFC20E`、红橙 `#F48221`、紫 `#8C5BA8`
- 实现：以产品图/Hermes 实测四色为源锚点，逐像素归最近色块并替换为目标色；黑色 `newgen` 字与透明底保留
- 复核：色块绿/黄/红橙/紫均正确，文字完整，透明干净（视觉目检通过）

> 注：用户消息直接采用示例 4 HEX；若后续需与实物严格一致，可再提供精确色覆盖。

## 执行任务（按序执行，回报表见文末）

### 任务 1 · 推送代码
```
git push
```
预期：origin/master 同步（含 `7dc055f` 与新 `newgen.png`）。

### 任务 2 · 清缓存构建并部署
```
rm -rf .next .open-next && npm run build:cf:static && npm run deploy
```
预期：构建成功部署。

### 任务 3 · 浏览器目检
1. NEWGEN 卡四色块 = 绿 #8EC43F / 黄橙 #FFC20E / 红橙 #F48221 / 紫 #8C5BA8
2. 文字黑色完整、透明底干净、卡片无溢出
3. 截图回报观感；若与实物仍有偏差，提供精确 HEX 我再覆盖
4. 记录仍不理想项

---

## 执行回报（Hermes 填写）

| 任务 | 结果 | 说明 |
|------|------|------|
| 1 推送代码 | 待执行 | |
| 2 构建+部署 | 待执行 | |
| 3 浏览器目检 | 待执行 | |

---

## 历史备注（供参考，无需执行）

- ✅ 批次71：NEWGEN 彩色四色块标，已部署 v`7dac6bc0`（本批修正色值为精确 HEX）。
- ✅ 批次70：Finnmark 官方配色。
- ✅ 批次69、68：箭头避让 / Beem 放大。
- 待办：社媒 URL 换品牌账号。
- 保持既有约定：不改 `wrangler.toml`、不动 DNS、不整库 `db:deploy`。
