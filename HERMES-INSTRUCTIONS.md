# Hermes 操作指令（Claude Code 下发）

> 批次: **第五十三批（最新）** · 更新: 2026-09-06 · 来源: Claude Code
> 状态: **logo 克莱因蓝 + 按钮灰色 · 待执行部署**

---

## 第五十三批：logo 克莱因蓝 #002FA7 / 按钮灰 #2b3c3d

用户反馈：logo 采用克莱因蓝、按钮用灰色 `#2b3c3d`。

CC 已落地（提交 `e262bae`）：
- `public/assets/logo-blue-solid.png` 重新着色为**克莱因蓝 #002FA7**（页头/页脚引用不变）
- `globals.css` 主按钮改**灰 `#2b3c3d` 底 + 米白字**，hover `#222e2f`（文字从深墨改米白，保证深灰底对比度）
- 其余（页头/页脚“富宁电子”楷体、配色结构）不变

## 执行任务（按序执行，回报表见文末）

### 任务 1 · 推送代码
```
git push
```
预期：origin/master 同步（含提交 `e262bae`）。

### 任务 2 · 清缓存构建并部署
```
rm -rf .next .open-next && npm run build:cf:static && npm run deploy
```
预期：构建成功部署。

### 任务 3 · 产物抽查
```
curl -s <ROOT>/ | grep -c "2b3c3d"                # 预期 ≥1（按钮灰进 CSS）
curl -sI <ROOT>/assets/logo-blue-solid.png | head -1  # 预期 200
```
<ROOT> 为站点根域（`https://fnec.net`）。

### 任务 4 · 浏览器目检（中英双语）
1. logo 实测主色为**克莱因蓝 #002FA7**（rgb 0,47,167）
2. 全站主按钮为**灰 #2b3c3d 底 + 米白字**，hover 变深；照片/浅底上均可读
3. 页头/页脚“富宁电子”字标仍正常（楷体回退）
4. 记录仍不理想项

---

## 执行回报（Hermes 填写）

| 任务 | 结果 | 说明 |
|------|------|------|
| 1 推送代码 | 待执行 | |
| 2 构建+部署 | 待执行 | |
| 3 产物抽查 | 待执行 | |
| 4 浏览器目检 | 待执行 | |

---

## 历史备注（供参考，无需执行）

- ✅ 批次52：字标弃书法字体改楷体回退，已处理。
- ✅ 批次51：火星橙按钮 + 深蓝 logo + “富宁电子”字标（后弃书法）。
- ✅ 批次50：换图 + logo/按钮配色迭代，已部署 v`6f61bf6b`。
- 保持既有约定：不改 `wrangler.toml`、不动 DNS、不整库 `db:deploy`。
