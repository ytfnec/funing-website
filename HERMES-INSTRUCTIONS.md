# Hermes 操作指令（Claude Code 下发）

> 批次: **第五十八批（最新）** · 更新: 2026-09-06 · 来源: Claude Code
> 状态: **品牌与交互样式统一 · 待执行部署**

---

## 第五十八批：logo #2257c9 / 字标全称随页面字体 / 控件统一按钮灰

用户反馈：logo 配色 `#2257c9`；“富宁电子”改为“烟台富宁电子”且字体与页面一致；中英文切换按钮、流程 12345 圆形、报价 1234 圆形与按钮颜色一致；并排查其它页面一并统一。

CC 已落地（提交 `004258d`，8 文件）：

1. **logo** `public/assets/logo-blue-solid.png` 重新着色为 **#2257c9**（页头/页脚引用不变）
2. **字标**页头/页脚文字 → **“烟台富宁电子”**；`.brand-zh` 移除特设字体与 600 字重，`font-family: inherit`（与页面正文一致、黑色）
3. **统一控件色**（新增 CSS 变量 `--btn-bg:#2b3c3d / --btn-fg:#f5efe4`，控件统一用按钮灰）：
   - 中英文切换（LanguageSwitcher）：激活项改灰底米白字，容器描边改暖黑（原为白色描边/金色激活）
   - OEM 流程圆形 01–05：灰底米白字
   - 报价页 1234 步骤圆 + 进度连接线：灰底/灰线
   - 报价页选中产品卡/数量卡：灰边框 + 灰淡底（原金色）
   - /products 分类筛选激活态：灰底米白字

## 执行任务（按序执行，回报表见文末）

### 任务 1 · 推送代码
```
git push
```
预期：origin/master 同步（含提交 `004258d`）。

### 任务 2 · 清缓存构建并部署
```
rm -rf .next .open-next && npm run build:cf:static && npm run deploy
```
预期：构建成功部署。

### 任务 3 · 产物抽查
```
curl -sI <ROOT>/assets/logo-blue-solid.png | head -1    # 预期 200
curl -s <ROOT>/ | grep -c "烟台富宁电子"                  # 预期 ≥1（页头或页脚）
```
<ROOT> 为站点根域（`https://fnec.net`）。

### 任务 4 · 浏览器目检（中英双语）
1. logo 实测主色 **#2257c9**（rgb 34,87,201）
2. 页头（≥768px）/页脚“烟台富宁电子”，黑体/页面字体一致
3. 中英文切换激活项 = 灰底米白字；OEM 流程圆、报价步骤圆、分类筛选激活态均灰底米白字
4. 报价页选中的产品卡/数量卡为灰边框
5. 记录仍不理想项（例如残留的琥珀/金装饰，若需一并处理可记录）

---

## 执行回报（Hermes 填写）

| 任务 | 结果 | 说明 |
|------|------|------|
| 1 推送代码 | 待执行 | |
| 2 构建+部署 | 待执行 | |
| 3 产物抽查 | 待执行 | |
| 4 浏览器目检 | 待执行 | |

---

## 历史备注（供参考，无需执行）

- ✅ 批次57：蓝牙音响 + 全站 8051/ARM，已部署 v`6484d886`。
- ✅ 批次56：桑拿控制系统文案更新。
- ✅ 批次55：富宁电子黑体 + logo #001489（已部署）。批次54/53/52/51/50 均完成。
- 保持既有约定：不改 `wrangler.toml`、不动 DNS、不整库 `db:deploy`。
