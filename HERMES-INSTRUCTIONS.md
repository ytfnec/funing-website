# Hermes 操作指令（Claude Code 下发）

> 批次: **第六十二批（最新）** · 更新: 2026-09-07 · 来源: Claude Code
> 状态: **hero 加载黑屏修复 · 待执行部署**

---

## 第六十二批：hero 首屏加载先全黑 → 修复为暖色占位

问题：切换到首页时 hero 先全黑再显示照片。
原因：hero 大图(1920 webp, 316KB)加载完成前显示的是 **blur 占位图**——该占位是从**旧的暗色健身房图**生成的 16×16 近黑 PNG(平均亮度 21)，加上 hero 区块底色近黑 `#15120e`，所以加载间隙整块呈黑色。

CC 已改（提交 `8149c2e`，`src/app/page.tsx`）：
- 用当前 hero（Terra_11 户外暖光桑拿）重新生成 **24×14 暖色模糊占位**（平均亮度 ~100，PNG ~748B 内联 base64）
- Next/Image `placeholder="blur"` 在加载期间即显示暖色虚影，不再全黑

## 执行任务（按序执行，回报表见文末）

### 任务 1 · 推送代码
```
git push
```
预期：origin/master 同步（含提交 `8149c2e`）。

### 任务 2 · 清缓存构建并部署
```
rm -rf .next .open-next && npm run build:cf:static && npm run deploy
```
预期：构建成功部署。

### 任务 3 · 浏览器目检
1. 首次加载/切换到首页：hero 区域先显示**暖色模糊图**，随后清晰照片淡入，**无全黑间隙**
2. 弱网/限速 3G 下同样无黑屏（可 DevTools 限速验证）
3. hero 白字/按钮在加载前后均可读（占位为暖调、底色仍深咖兜底）
4. 记录仍不理想项

---

## 执行回报（Hermes 填写）

| 任务 | 结果 | 说明 |
|------|------|------|
| 1 推送代码 | 待执行 | |
| 2 构建+部署 | 待执行 | |
| 3 浏览器目检 | 待执行 | |

---

## 历史备注（供参考，无需执行）

- ✅ 批次61：字标中文“烟台富宁”/英文“YANTAI FUNING”，已部署 v`8326a850`。
- ✅ 批次60：授权品牌整机新卖点 + 详情页 Key Features bug 修复，已部署 v`9823d352`。
- 保持既有约定：不改 `wrangler.toml`、不动 DNS、不整库 `db:deploy`。
